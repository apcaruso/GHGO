# Architecture

ghgo is planned as a local-first desktop application for greenhouse gas reporting.

The project uses Go with SQLite for local storage. A Fyne desktop UI is planned for a later step, but this skeleton intentionally has no UI and no web backend.
