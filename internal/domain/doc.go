// Package domain contains ghgo's storage-independent core data types.
//
// It deliberately avoids dependencies on persistence, UI, import, parsing, and
// calculation implementations.
package domain
